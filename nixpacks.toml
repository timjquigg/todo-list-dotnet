[phases.setup]
nixPkgs = ['dotnet-sdk_7', 'nodejs']

[phases.install]
cmd = 'npm ci'

[phases.build]
cmd = 'npm run build'

[phases.start]
cmd = 'dotnet server/bin/Debug/net7.0/publish/todo-dotnet-api.dll'